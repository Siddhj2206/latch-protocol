{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "test": "bun test",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@latch-protocol/core": "workspace:*",
    "@latch-protocol/db": "workspace:*",
    "@latch-protocol/env": "workspace:*",
    "dotenv": "catalog:",
    "hono": "^4.12.32",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@latch-protocol/config": "workspace:*",
    "@types/node": "catalog:",
    "bun-types": "^1.4.0",
    "tsdown": "^0.22.14",
    "typescript": "catalog:"
  }
}
